<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zh.sensor">

    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HeartRateActivity"></activity>
        <activity android:name=".PositionSensorsActivity" />
        <activity android:name=".EnvironmentActivity" />
        <activity android:name=".GyroscopeSensorActivity" />
        <activity android:name=".AccelerometerSensorActivity" />
        <activity android:name=".GravitySensorActivity" />
        <activity android:name=".OrientationSensorActivity" />
        <activity android:name=".RotationVectorDemo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>